# 전 세계 개발자와 코드 공유하기, NPM 패키지 배포(Publish)와 업데이트

### 1. 로그인 및 인증 (Authentication)

- CLI 회원 인증: npm login 명령어로 진행합니다.
- 보안 원칙 (2FA): 계정 도용 및 악성 코드 유포를 방지하기 위해 이메일/앱 기반 OTP(일회용 비밀번호) 인증을 필수적으로 수행

### 2. 패키지 설정 및 스코프 (Scope)

- 패키지 이름 충돌 방지: 기존 패키지와 이름 중복을 피하기 위해 Scope 활용 권장.

```
 예: @사용자아이디/패키지명 형태로 지정 (@yongsoo/my-awesome-package).
```

- 주요 필드 체크:

```
name: 패키지 고유 이름
version: 유의적 버전 (1.0.0)
description: 패키지 설명
main: 진입점 파일 (index.js)
type: 모듈 시스템 설정 (module)
```

### 3. 패키지 배포

- 설정 완료후 아래 명령어 실행

```
npm publish
```

### 4. 패키지 업데이트 및 버전 관리

- 코드 불변성 (Immutability)
  한 번 배포된 버전은 기존 사용자의 안정성을 위해 덮어쓰기가 원천 금지됨 (E403 에러 발생).
  코드를 수정하거나 버그를 잡은 경우 반드시 버전을 올린 후 재배포해야 함.

| 구분      | 명령어              | 설명                                               | 예시              |
| :-------- | :------------------ | :------------------------------------------------- | :---------------- |
| **Patch** | `npm version patch` | 간단한 버그 수정 (맨 끝자리 증가)                  | `1.0.0` → `1.0.1` |
| **Minor** | `npm version minor` | 하위 호환 가능한 신규 기능 추가 (가운데 자리 증가) | `1.0.0` → `1.1.0` |
| **Major** | `npm version major` | 기존 코드와 호환되지 않는 큰 변화 (맨 앞자리 증가) | `1.0.0` → `2.0.0` |
