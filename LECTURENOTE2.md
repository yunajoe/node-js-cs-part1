# NPM과 Git 협업, 그리고 유의적 버전(Semantic Versioning)의 원리

### 1. 개요 및 배경

- Git의 존재 이유: 소스 코드 변경 이력을 추적하고, 개발자 간 코드 충돌 없이 협업할 수 있도록 돕는 버전 관리 및 협업 인프라.
- 학습 목표: Git 환경에서 외부 패키지를 효율적으로 공유하고 관리하는 법, 그리고 패키지 버전 관리 규격을 이해하기.

---

### 2. .gitignore와 패키지 용량 최적화

- 용량이 매우 큰 node_modules 폴더는 원격 저장소(GitHub)에 올리지 않는 것이 원칙
- .gitignore 파일의 역할: Git 추적 대상에서 제외할 파일/폴더를 지정.
- 공유 메커니즘

```
node_modules/ 폴더는 .gitignore에 등록하여 업로드 제외.
가벼운 설계도인 package.json만 Git에 공유.
동료 개발자는 프로젝트 수신 후 npm install을 실행해 동일한 의존성 설치.

```

---

### 3. 유의적 버전 (Semantic Versioning, SemVer)

- 패키지 버전 번호(X.Y.Z)는 임의의 숫자가 아닌 규칙에 따라 부여됩니다.

| 구분                  | 의미    | 변경 시점                                          |
| :-------------------- | :------ | :------------------------------------------------- |
| **Major (주 버전)**   | `5`.0.0 | 하위 호환성이 깨지는 대규모 변경 및 사용법 수정 시 |
| **Minor (부 버전)**   | 5.`3`.0 | 하위 호환성을 유지하면서 새로운 기능 추가 시       |
| **Patch (수정 버전)** | 5.3.`0` | 기존 기능의 버그 및 보안 취약점 수정 시            |

- 하위 호환성(Backward Compatibility)
  버전 업그레이드가 진행되어도 기존 작성된 코드가 문제없이 작동하는 성질. Major 버전이 상승하면 하위 호환성이 파괴되므로 주의가 필요합니다.

---

### 4. 버전 범위 기호 및 업데이트 전략

- package.json 버전 표기 앞의 기호는 자동 업데이트 범위를 지정

#### 기호별 표기법

- **`^` (Caret):** Major 버전을 고정하고, Minor 및 Patch 버전 자동 업데이트 허용 (예: `^5.3.0` $\rightarrow$ `5.3.0` 이상 `6.0.0` 미만)
- **`~` (Tilde):** Major와 Minor 버전을 고정하고, Patch 버전만 자동 업데이트 허용 (예: `~5.3.0` $\rightarrow$ `5.3.0` 이상 `5.4.0` 미만)

- 특정 버전 명시 설치 명령어:

```bash
# 강제로 최신 Major 버전까지 설치
npm install chalk@latest

# 특정 버전을 정확히 지정하여 설치
npm install chalk@4.1.2

```
