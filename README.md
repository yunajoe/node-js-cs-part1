# Node.js의 본질과 운영체제 기반의 런타임 환경 이해

### 1. Node.js의 정의: 언어가 아닌 '런타임'

- Node.js는 새로운 프로그래밍 언어나 프레임워크가 아닙니다.
- 자바스크립트 코드를 컴퓨터 운영체제(OS) 위에서 직접 실행할 수 있게 해주는 **'런타임 환경(Runtime Environment)'**입니다.

---

### 2. 기술적 혁신: V8 엔진과 C++의 결합

- 브라우저 환경의 보안 정책(샌드박스)으로 인해 자바스크립트는 파일 제어나 메모리 접근 등 시스템 레벨 작업이 원천적으로 불가능했습니다.
- 구글 크롬의 고성능 자바스크립트 해석기인 **V8 엔진**을 추출한 뒤, 컴퓨터 OS와 직접 소통할 수 있는 **C++ 프로그램**으로 감싸 안았습니다.
- 자바스크립트에 C++라는 강력한 시스템 제어용 '수트'를 입혀, **파일 시스템 조작 및 네트워크 포트 개방 권한**을 부여했습니다.

---

### 3. I/O 집약적 vs CPU 집약적

| 분류             | 📥 I/O 집약적 서비스 (I/O Bound)                                   | ⚙️ CPU 집약적 서비스 (CPU Bound)                                                      |
| :--------------- | :----------------------------------------------------------------- | :------------------------------------------------------------------------------------ |
| **특징**         | 데이터의 입력과 출력(Input/Output)이 빈번함                        | CPU의 복잡한 수학적 연산이 오래 걸림                                                  |
| **예시**         | 실시간 채팅(디스코드), 스트리밍(넷플릭스), DB 읽기/쓰기            | 4K 영상 압축, 금융 통계 계산, AI 모델 학습                                            |
| **Node.js 성능** | ✨ **타의 추종을 불허함**<br>요청을 대기 없이 비동기로 가볍게 처리 | ⚠️ **도입에 매우 신중해야 함**<br>하나의 무거운 계산 동안 전체 서버가 멈춤 (Blocking) |

### 4. 코드 구조와 환경에 따른 권한의 변화 (이어서)

```javascript
// 프론트엔드와 백엔드에서 동일하게 동작하는 문법
function sayHello(name) {
  console.log(`안녕하세요, ${name}님!`);
}
sayHello("mike");
```

- 웹 브라우저에서 실행 시
  - OS 파일 시스템이나 네트워크 포트에 직접 접근이 **불가능**합니다.
  - 브라우저가 허가한 화면 UI 조작, 애니메이션 처리 등에만 활용됩니다.
- Node.js 환경에서 실행 시:
  - 자바스크립트 문법 그대로 **OS의 권한**을 빌려올 수 있습니다.
  - `fs` 모듈로 하드디스크에 파일을 저장하거나, `http` 모듈로 수만 명의 접속을 받는 웹 서버 포트를 직접 열 수 있습니다.

---

### 5. 핵심 용어 사전 (Glossary)

#### 5-1) 샌드박스 (Sandbox)

- **단순 의미:** 외부 접근을 차단하여 보호된 격리 구역.
- **실무적 맥락:** 해킹 사이트의 자바스크립트 코드가 내 컴퓨터를 포맷할 수 없는 이유입니다. 브라우저는 자바스크립트를 철저히 모래성(샌드박스) 안에 가둡니다. Node.js는 이 샌드박스를 부수고 자바스크립트에게 **'시스템 제어권'**을 쥐여준 사건입니다.

#### 5-2) 런타임 환경 (Runtime Environment)

- **단순 의미:** 특정 프로그래밍 언어로 작성된 코드가 실행되는 토대.
- **실무적 맥락:** 문법은 텍스트일 뿐이며, 이를 컴퓨터가 알게 실행해 주는 토대입니다. 브라우저가 제공하는 도구(`window`, `document`)와 Node.js가 제공하는 도구(`require('fs')`)가 다른 이유입니다.
  > 💡 **비유:** 똑같은 한국어를 쓰더라도 방송국에 가면 마이크를 쥐고, 주방에 가면 칼을 쥐는 것과 같습니다.

#### 5-3) API (Application Programming Interface)

- **단순 의미:** 프로그램들이 서로 상호작용하기 위한 통신 인터페이스.
- **실무적 맥락:** 백엔드 개발 관점에서의 API는 **'식당의 메뉴판이자 점원'**입니다. 클라이언트가 DB에 직접 접근하면 위험하므로, *"특정 요청을 주면 데이터를 주겠다"*고 열어놓은 명확한 창구입니다.

#### 5-4) 병목 현상 (Bottleneck)

- **단순 의미:** 전체 시스템 성능이 특정 느린 요소 하나 때문에 제한받는 현상.
- **실무적 맥락:** 4차선 도로가 1차선으로 줄어드는 것과 같습니다. Node.js에서 단 하나의 무거운 CPU 연산이 실행되면 뒤이어 줄 선 수천 명의 로그인 요청이 멈춥니다. 아키텍트는 이런 무거운 연산을 파악하고 Python 서버 등으로 분리(**MSA**)하는 설계를 해야 합니다.

#### 5-5) 스레드 (Thread)

- **단순 의미:** 프로세스 내에서 실제로 작업을 수행하는 가장 작은 흐름의 단위.
- **실무적 맥락:** 식당의 종업원에 비유할 수 있습니다.
  - **Java/C++ (Multi-thread):** 손님이 올 때마다 종업원을 새로 고용해 1:1로 응대합니다.
  - **Node.js (Single-thread):** 아주 똑똑한 **단 한 명의 종업원**이 주문(I/O)만 재빠르게 받아 주방(OS)에 넘기고, 요리가 완료되면 알림(Event)을 받아 서빙하는 구조입니다. 가벼운 주문 처리는 폭발적이지만, 종업원이 직접 요리(CPU 연산)를 시작하면 다른 손님의 주문이 모두 막히는 치명적 약점이 있습니다.
