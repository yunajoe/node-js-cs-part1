# 전역 패키지(Global Packages)의 한계와 모던 실행 도구 npx의 원리

### 1.전역 패키지(Global Package) 개념과 한계

- '전 세계적인' 또는 '전체적인'이라는 뜻을 가지고 있습니다. 따라서 전역 패키지, 즉 Global Package란 내 컴퓨터 시스템 전체에 딱 한 번만 설치하여 모든 프로젝트가 공동으로 사용할 수 있도록 만든 패키지를 의미

- 과거에 개발자들은 코드를 수정할 때마다 서버를 껐다 켜주는 nodemon 같은 유용한 도구들을 설치할 때, 터미널에 npm install nodemon --global 혹은 이를 줄여서 -g라는 옵션을 붙여 설치

```javascript
# 과거 방식: 내 프로젝트가 아닌, 컴퓨터 시스템 전체에 영구 설치
npm install nodemon --global

# (실무에서는 주로 -g 라고 짧게 줄여서 썼습니다)
npm install nodemon -g

// 이렇게 전역 옵션을 붙이면 패키지가 현재의 작업 폴더가 아닌, 운영체제의 깊숙한 공용 시스템 폴더에 설치되어 컴퓨터 어디서든 명령어 하나로 실행할 수 있는 편리함을 제공했습니다.

```

```
[컴공 기초 레벨업: 지역(Local)과 전역(Global), 그리고 환경변수(PATH)]
컴퓨터 공학에서 '지역(Local)'은 내 방(특정 폴더) 안에서만 쓸 수 있는 개인 물건이고, '전역(Global)'은 거실에 두고 온 가족이 함께 쓰는 공용 물건입니다. -g 옵션으로 패키지를 설치하면, 컴퓨터의 운영체제(OS)는 이 프로그램의 위치를 '환경변수(PATH)'라는 시스템 지도에 기록해 둡니다. 덕분에 우리가 어느 폴더에서 터미널을 열든 컴퓨터가 그 프로그램의 위치를 바로 찾아 실행할 수 있는 것입니다.

```

- 한계 및 문제점

```
1. 의존성 지옥 (Dependency Hell): 컴퓨터의 전역(Global) 공간은 거실과 같아서 동일한 패키지의 버전을 딱 하나만 깔아둘 수 있습니다.
상황:
- 기존 레거시 프로젝트 A는 nodemon 1.0 버전이 필요함.
- 새로 시작하는 프로젝트 B는 최신 기능이 들어간 nodemon 3.0 버전이 필요함.

문제:
- 전역 버전을 3.0으로 올리는 순간, A 프로젝트를 실행하면 버전 호환성이 깨져 에러가 터진다.

2. 협업 시 환경 불일치: '내 컴퓨터에선 되는데요?' (협업 및 CI/CD 파이프라인 붕괴)
상황:
- 내 컴퓨터에는 과거에 설치해둔 전역 패키지(예: rimraf, pm2, ts-node 등)가 이미 깔려있어서 터미널에 명령어를 치면 잘 실행이 된다.
- 이 상태로 프로젝트 코드를 Git에 올리고 팀원에게 전달

문제:
- 전역으로 설치한 패키지는 프로젝트 설계도인 package.json에 기록이 전혀 남지 않습니다.
- 팀원 컴퓨터나 배포 서버(AWS, Vercel 등)에는 해당 전역 패키지가 없기 때문에, 명령어를 찾을 수 없다는 에러(command not found)가 발생하며 빌드가 깨집니다.

3. 유령 패키지 축적 & 최신 버전 미반영 (보안 및 용량 이슈)
- 문제: 전역으로 한 번 깔아둔 패키지는 개발자가 직접 npm update -g를 치지 않는 이상 옛날 버전에 그대로 고정됩니다.

```

npx는 컴퓨터에 영구적으로 남기지 않고, "프로젝트 안(node_modules)에 있는 버전을 먼저 쓰고, 없으면 NPM에서 최신 버전을 임시 캐시에 다운받아 딱 한 번 실행하고 깔끔하게 빠지는" 방식을 취합니다.
덕분에 프로젝트 간 버전이 꼬일 일도 없고, 팀원과 환경이 안 맞을 일도 없게 됩니다.

---

### 2. 모던 실행 도구: npx (Node Package Execute)

- 전역 설치의 단점을 완벽히 극복하는 일회성/격리형 실행 명령어
- 동작 프로세스

```
1단계: 현재 프로젝트의 node_modules에 해당 패키지가 있는지 확인 후 존재하면 즉시 실행합니다.
2단계: 없을 경우 NPM 중앙 저장소에서 최신 버전을 임시 캐시(Cache) 폴더로 다운로드합니다.
3단계: 프로젝트나 OS 전역 환경을 더럽히지 않고 실행을 마친 뒤 깔끔하게 정리합니다.
```

- 도입 이점

```
- 프로젝트 독립성 유지로 버전 충돌 차단
- 프레임워크 초기화 도구(npx create-next-app 등) 실행 시 항상 최신 버전 사용
- 불필요한 영구 설치를 줄여 디스크 용량 절약

```

```
[컴공 기초 레벨업: 캐시(Cache)란?]
컴퓨터 공학에서 캐시(Cache)는 '자주 쓰는 데이터를 임시로 보관해 두는 아주 빠른 보관소'를 뜻합니다. 매번 무거운 정식 설치 과정을 거치지 않고, 한 번 인터넷에서 받아온 파일을 캐시라는 임시 창고에 잠시 놔두었다가 다음번에 요청이 들어오면 설치 없이 창고에서 바로 꺼내 실행하는 원리입니다.

```

3. 차세대 패키지 매니저: pnpm (Performant NPM)

- NPM의 고질적인 문제인 node_modules 중복 저장 및 디스크 낭비를 해결한 도구입니다.

| 구분          | 기존 npm                                           | pnpm                                                  |
| :------------ | :------------------------------------------------- | :---------------------------------------------------- |
| **저장 방식** | 프로젝트마다 동일 패키지를 복사해 중복 저장        | PC 내 **중앙 저장소(Global Store)**에 원본 1회만 저장 |
| **연결 방식** | 디렉토리 내 실제 파일 존재 (`node_modules` 비대화) | **하드 링크 / 심볼릭 링크**를 통해 원본 연결          |
| **장점**      | 구조가 단순함                                      | **설치 속도 2~3배 향상**, **디스크 용량 절감**        |
