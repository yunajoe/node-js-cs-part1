# 브라우저를 탈출한 자바스크립트: C++와 V8 엔진이 만든 런타임의 기적

## 1. 콜 스택(Call Stack) — 단 하나의 작업 공간

자바스크립트 엔진(V8) 내부에는 변수가 저장되는 창고(Memory Heap)와 실제로 코드가 실행되는 단 하나의 책상(Call Stack)이 존재합니다. 코드가 실행된다는 것은 이 책상 위에 서류가 올라왔다는 뜻입니다.

- **후입선출(LIFO, Last In First Out)**: 나중에 들어온 서류가 책상 맨 위에 쌓이고, 그것부터 먼저 처리되어 나가는 구조
- **블로킹(Blocking)의 발생**: 메인 스레드가 오직 1개이므로, 이 스택에 용량이 큰 파일 읽기(`fs.readFileSync`)가 올라가면 소장님이 그것을 끝낼 때까지 현장 전체가 멈춥니다.

> 💡 **CS 실무 Point — Call Stack Size Exceeded**
> 재귀 함수(Recursive Function)를 잘못 작성하여 탈출 조건을 주지 않으면, 함수가 콜 스택에 무한히 쌓이다가 결국 운영체제가 할당한 메모리 한계치를 넘어 폭발하는 `Maximum call stack size exceeded` 에러가 발생하며 서버가 크래시(Crash)됩니다.

> 🌱 **직관적 비유**
> 콜 스택은 총괄 현장 소장님의 '책상'입니다. 소장님은 서류를 차곡차곡 쌓아두고 위에서부터 처리합니다. 만약 10GB짜리 도면을 직접 처음부터 끝까지 다 읽고 있다면(동기식 블로킹), 다른 서류는 만지지도 못하고 현장이 마비됩니다.

---

## 2. 립유브이(libuv) — 대형 하청업체와 스레드 풀

고독한 소장님을 돕기 위해 Node.js가 외부에서 고용한 C 언어 기반의 강력한 하청업체 라이브러리입니다.

- **스레드 풀(Thread Pool)**: libuv 내부에 존재하는 여러 명의 백그라운드 일꾼(스레드)들로, 파일 입출력, 암호화 등 무거운 작업을 메인 스레드 대신 병렬로 처리합니다.
- **하이브리드 시스템**: 개발자가 코드를 짜는 환경은 싱글 스레드이지만, 시스템 밑바탕에서는 libuv가 멀티 스레드로 동작합니다.

> 💡 **CS 실무 Point — 스레드 풀 크기 튜닝**
> 기본적으로 libuv의 스레드 풀은 4개의 스레드만 갖습니다. 이미지 리사이징 요청이 동시에 10개가 들어오면 6개는 대기하게 되어 지연이 발생합니다. 시니어 엔지니어는 서버의 CPU 코어 수에 맞춰 `UV_THREADPOOL_SIZE` 환경변수를 튜닝하여 이 병목을 해소합니다.

> 🌱 **직관적 비유**
> libuv는 든든한 '대형 하청업체 시공반'입니다. 소장님이 무거운 철근을 옮기라는 지시를 발견하면, 직접 하지 않고 libuv에게 서류를 넘깁니다. 일꾼들이 땀 흘려 일하는 동안 소장님은 즉시 다음 설계도를 검토할 수 있습니다.

---

## 3. 이벤트 큐(Event Queue) — 작업 완료 보고서 대기함

libuv에서 비동기 작업을 마친 후, "소장님, 작업 끝났습니다!"라고 보고하는 콜백 함수(Callback)들이 차례대로 대기하는 공간입니다.

- **선입선출(FIFO, First In First Out)**: 책상(콜 스택)의 규칙과 반대로, 대기함은 먼저 들어온 콜백이 먼저 나가는 줄서기 규칙을 따릅니다.

> 💡 **CS 실무 Point — 분업화된 대기함**
> 이 대기함은 사실 거대한 하나의 통이 아닙니다. 알람 보고서 전용, 파일 읽기 보고서 전용 등 작업의 성격에 따라 총 6개의 전용 대기함으로 세분화되어 있으며, 이는 이벤트 루프의 6단계 페이즈와 연결됩니다.

---

## 4. 이벤트 루프(Event Loop) — 깐깐한 순찰 반장

대기함에 쌓인 보고서들을 언제 소장님 책상으로 올려보낼지 결정하는 무한 회전 감시 시스템입니다. 이벤트 루프는 소장님의 콜 스택이 완전히 텅 비었을 때만 움직입니다. 메인 스레드가 찰나의 휴식을 취할 때, 대기함에서 콜백을 하나 꺼내 소장님 책상으로 밀어 올립니다.

---

## 비교: 콜 스택 vs 이벤트 큐

| 비교 항목     | 콜 스택 (Call Stack)                                          | 이벤트 큐 (Event Queue)                                       |
| ------------- | ------------------------------------------------------------- | ------------------------------------------------------------- |
| **주요 역할** | 현재 실행 중인 함수들을 관리하는 실제 작업 공간               | 비동기 작업이 끝나고 실행되기를 기다리는 콜백 함수들의 대기실 |
| **자료 구조** | LIFO (후입선출) — 나중에 들어온 것이 맨 위에 쌓이고 먼저 나감 | FIFO (선입선출) — 먼저 줄을 선 콜백이 먼저 나감               |
| **지배 주체** | 자바스크립트 엔진 (V8 등)                                     | 브라우저(Web API) 또는 Node.js (libuv)                        |
| **실행 시점** | 코드가 읽히는 순간 즉시 실행                                  | 메인 스레드의 콜 스택이 완전히 비어 있을 때만 실행 가능       |

---

## 깊이 있는 개념 비교

### 콜 스택(Call Stack) — 소장님의 '책상'

자바스크립트 엔진은 하나의 메인 스레드(일꾼)와 하나의 콜 스택(책상)만 가집니다.

- 코드를 한 줄씩 읽으면서 함수를 실행할 때마다 콜 스택에 스택 프레임을 차곡차곡 위로 쌓아 올립니다.
- 함수가 값을 리턴하고 종료되면 스택의 맨 위에서부터 쏙 빠져나갑니다.
- **특징**: 여기에 무거운 작업(예: 용량이 큰 이미지 처리, 동기식 DB 조회 등)이 올라오면, 그 작업이 끝날 때까지 아래에 깔린 다른 함수들은 실행되지 못하고 멈춥니다(블로킹 현상).

### 이벤트 큐(Event Queue / Task Queue) — 완료 보고서 '대기함'

`setTimeout`, 네트워크 요청, 파일 읽기 같은 비동기 작업의 결과물(콜백 함수)들이 메인 스레드로 들어가기 위해 한 줄로 서서 기다리는 대기열입니다.

- 백그라운드(하청업체)에서 작업이 끝났다는 신호가 오면, 해당 작업의 콜백 함수가 이벤트 큐의 맨 뒤에 등록됩니다.
- **특징**: 아무리 대기실(큐)에 콜백이 가득 차 있어도, 작업 공간(콜 스택)에 서류가 단 한 장이라도 남아있다면 절대로 책상 위로 올라갈 수 없습니다.

---

## 두 부품을 조율하는 심판: 이벤트 루프(Event Loop)

콜 스택과 이벤트 큐가 유기적으로 돌아가게 만드는 것이 바로 이벤트 루프입니다. 이벤트 루프는 1초에 수만 번씩 돌며 오직 한 가지 규칙만 감시합니다.

> 🔄 **이벤트 루프의 골든 룰**
> "콜 스택(책상)이 완전히 비었는가? 그렇다면 이벤트 큐(대기실) 맨 앞에 있는 녀석을 하나 꺼내서 콜 스택으로 밀어 올려라!"

### 코드로 보는 흐름 예시

```javascript
console.log("1");
setTimeout(() => console.log("2"), 0);
console.log("3");
```

1. `console.log("1")`이 콜 스택에 들어와 즉시 실행되어 화면에 `1`이 찍히고 나갑니다.
2. `setTimeout`이 콜 스택에 들어옵니다. 비동기 함수이므로 타이머를 백그라운드에 던져두고 콜 스택에서 즉시 빠집니다. 0초 뒤 타이머가 끝나면 `() => console.log("2")` 콜백 함수가 이벤트 큐에 들어와 대기합니다.
3. `console.log("3")`이 콜 스택에 들어와 실행되어 화면에 `3`이 찍히고 나갑니다.
4. 이제 콜 스택이 완전히 텅 빕니다.
5. 이때를 기다리던 이벤트 루프가 이벤트 큐에서 대기 중이던 콜백을 꺼내 콜 스택으로 밀어 올립니다.
6. 마지막으로 `console.log("2")`가 실행되며 화면에 `2`가 찍힙니다.

**최종 출력**: `1` → `3` → `2`

> 💡 **면접용 요약 답변**
> "콜 스택은 자바스크립트 엔진이 현재 동기적으로 처리해야 하는 작업을 관리하는 후입선출(LIFO) 구조의 실제 실행 공간인 반면, 이벤트 큐는 비동기 작업이 완료된 후 실행을 기다리는 콜백 함수들이 선입선출(FIFO) 구조로 대기하는 공간입니다. 이벤트 루프는 콜 스택이 완전히 비었을 때 이벤트 큐의 콜백을 스택으로 이동시켜 실행을 이어 나갑니다."

---

## 콜 스택의 용량

콜 스택(Call Stack)도 엄연히 메모리 공간을 차지하기 때문에 '총 용량(제한)'이 존재하며, 이를 초과하면 여지없이 터져버립니다.

이 현상을 바로 **스택 오버플로우(Stack Overflow)**라고 부르며, 자바스크립트(Node.js / 브라우저) 환경에서는 `RangeError: Maximum call stack size exceeded`라는 에러 메시지를 뿜으며 프로그램이 즉시 크래시됩니다.

### 1) 콜 스택은 정확히 '언제' 터질까?

함수가 호출될 때마다 콜 스택에는 스택 프레임(Stack Frame)이라는 메모리 조각이 하나씩 쌓입니다. 이 조각 안에는 다음과 같은 정보가 들어있습니다.

- 함수의 매개변수(Arguments)
- 함수 내부에서 선언된 지역 변수
- 함수가 끝나면 돌아갈 원래 코드의 주소(Return Address)

이 스택 프레임들이 쌓이다가 엔진이나 OS가 설정해 둔 스택 메모리의 한계치를 넘는 순간 터집니다. 가장 대표적인 원인은 탈출 조건이 없거나 잘못된 **재귀 함수(Recursive Function)**입니다.

```javascript
function 양파썰기() {
  // 탈출 조건이 없어서 함수가 함수를 무한히 호출함
  양파썰기();
}

양파썰기(); // 🚨 실행하자마자 스택 오버플로우 발생!
```

### 2) 콜 스택의 '총 용량'은 얼마일까?

자바스크립트 명세(ECMAScript)에는 "콜 스택의 용량은 정확히 얼마여야 한다"라고 정해져 있지 않습니다. 즉, 자바스크립트 엔진(V8 등)의 종류, 운영체제(OS), 심지어 실행 환경(브라우저냐 Node.js냐)에 따라 총 용량이 다릅니다.

바이트(Byte) 단위의 용량보다는 "함수를 최대 몇 개까지 쌓을 수 있는가(최대 호출 스택 크기)"로 한계를 정하는 경우가 많습니다.

| 엔진 / 환경                | 대략적인 한계        |
| -------------------------- | -------------------- |
| V8 엔진 (Node.js & Chrome) | 약 10,000 ~ 14,000회 |
| Safari (WebKit 엔진)       | 약 30,000 ~ 40,000회 |

> 💡 **재미있는 사실**
> 함수 내부에 지역 변수를 엄청나게 많이 선언해 두면, 스택 프레임 하나의 크기가 커지기 때문에 10,000번을 채우지 못하고 5,000번 만에도 터질 수 있습니다. 즉, **[함수 개수 × 함수의 메모리 크기]**가 총 용량을 넘으면 터집니다.

---

## 5. 이벤트 루프의 6단계 페이즈(Phases)

이벤트 루프(순찰 반장)는 하나의 대기함만 보는 것이 아니라, 특정 작업의 독점을 막기 위해 총 6개의 분리된 정거장을 순서대로 도는 순환 버스를 타고 현장을 관리합니다.

| 단계 | 정거장 이름       | 처리 내용                                                                                                            |
| ---- | ----------------- | -------------------------------------------------------------------------------------------------------------------- |
| 1    | Timer             | `setTimeout`, `setInterval` 등 약속된 시간이 지난 알람 콜백을 처리                                                   |
| 2    | Pending Callbacks | TCP 오류 등 운영체제 레벨의 긴급한 에러를 처리                                                                       |
| 3    | Idle, Prepare     | 내부 정비 시간 (개발자 코드는 실행되지 않음)                                                                         |
| 4    | Poll              | 가장 중요하고 바쁜 정거장. 파일 읽기, DB 응답 등 대부분의 I/O 콜백이 모임. 할 일이 없으면 새 일거리가 올 때까지 대기 |
| 5    | Check             | `setImmediate()` 전용 창구                                                                                           |
| 6    | Close Callbacks   | 소켓 연결 종료 등 메모리 누수 방지를 위한 뒷정리                                                                     |

> 💡 **강사 팁**
> `setTimeout(0)`이라고 적어도 운영체제 타이머의 물리적 한계로 인해 정확히 0ms 만에 실행됨을 보장하지는 않습니다. 보통 최소 1ms 이상의 지연이 발생합니다.

> 💡 **CS 실무 Point — `setTimeout(0)` vs `setImmediate()`**
> 파일 읽기(Poll 정거장) 내부에서 이 둘을 동시에 호출하면, 순환 버스는 Poll에서 출발하여 항상 다음 정거장인 Check(5번) 정거장에 먼저 도착하므로 무조건 `setImmediate()`가 `setTimeout(0)`(1번 정거장)보다 먼저 실행됨을 보장받을 수 있습니다.

---

## 6. [매우 중요] 절대 권력의 VIP 대기열과 새치기 현장

6단계 정거장의 룰을 완벽하게 박살 내고 새치기하는 절대 권력의 대기열이 있습니다. 이를 **마이크로태스크 큐(Microtask Queue)**라고 부릅니다.

### 👑 엄격한 VIP 우선순위 체계 (외워두세요!)

| 순위              | 명칭              | 대상                                                       |
| ----------------- | ----------------- | ---------------------------------------------------------- |
| 0순위 (VVIP)      | 넥스트틱 큐       | `process.nextTick()`의 콜백 — 현장에서 가장 급한 재난 알림 |
| 1순위 (VIP)       | 마이크로태스크 큐 | `Promise`(`.then`, `async`/`await`)의 콜백                 |
| 2순위 (일반 시민) | 매크로태스크 큐   | 1~6번 정거장 서류들 (`setTimeout`, `fs.readFile` 등)       |

### 🚌 [상황극] 정거장 이동 중, VIP는 어떻게 새치기를 하는가?

이벤트 루프 버스는 1번(Timer) 정거장에서 일을 마치고 2번(Pending) 정거장으로 그냥 쿨하게 넘어가지 않습니다. 정거장과 정거장 사이를 이동할 때마다 무조건 급브레이크를 밟고 VIP 결재함(Microtask Queue)을 쳐다봅니다. 어떤 타이머 콜백 안에서 갑자기 Promise가 생성된 상황을 상상해 보십시오.

1. 버스가 1번 정거장(Timer)에 도착하여 대기 중이던 알람 서류를 소장님께 올립니다.
2. 그런데 소장님이 알람 서류를 처리하다 보니, 그 안에 "긴급 VIP 서류(Promise) 제출 요망!"이라는 코드가 있었습니다. 즉시 VIP 결재함에 서류가 하나 꽂힙니다.
3. 1번 정거장의 서류가 다 끝났습니다. 버스는 이제 2번 정거장(Pending)으로 출발하려고 액셀을 밟습니다.
4. **[새치기 발생!]** 액셀을 밟으려던 찰나, 버스는 급브레이크를 밟습니다! 방금 전 접수된 VIP 결재함에 불이 들어왔기 때문입니다.
5. 버스는 2번 정거장으로 가는 것을 멈추고, 즉시 VIP 결재함부터 열어서 방금 접수된 Promise 서류를 소장님께 올립니다.
6. VIP 결재함이 완전히 텅텅 빈 것을 확인한 후에야, 비로소 버스는 2번 정거장으로 출발합니다.

> 💡 **CS 실무 Point — 최신 Node.js의 극한의 VIP 우대**
> Node.js 11 버전 이후부터는 우선순위가 더욱 깐깐해졌습니다. 정거장을 이동할 때뿐만 아니라, 정거장 안에서 일반 서류를 딱 1장 처리할 때마다 매번 VIP 결재함을 쳐다봅니다. 1번 정거장에 서류가 10장 있어도, 1장 처리 후 VIP 서류가 생기면 나머지 9장을 버려두고 VIP부터 처리하는 극한의 새치기가 발생합니다.

### [시각화] 정거장 이동 중 VIP 새치기 발생의 순간

```javascript
setTimeout(() => {
  console.log("1. 알람 처리 중...");
  Promise.resolve().then(() => console.log("2. [새치기] 긴급 프로미스 생성!"));
}, 0);
```

**일반 1번 정거장(Timer) 처리 완료 직후의 순간**

```
[ Call Stack ]              : (알람 처리 완료로 텅 빔)
[ VIP 결재함 (Promise) ]     : [ '2. [새치기] 긴급 프로미스 생성!' ]  ← 방금 접수됨!
[ 일반 2번 정거장 (Pending) ] : (여기로 넘어가야 하지만...)
```

▶ **이벤트 루프의 행동**: "잠깐! 2번 정거장으로 가기 전에 VIP 함에 서류가 있네? 무조건 저것부터 콜 스택으로 올려!"

결과적으로 2번 정거장으로 가기 전, Promise가 중간에 가로채어 실행됩니다.

> 💡 **CS 실무 Point — 기아 현상(Starvation) 발생**
> 만약 개발자가 `process.nextTick`이나 `Promise`를 재귀적으로 끝없이 호출하면 어떻게 될까요? 이벤트 루프는 VVIP 서류만 쳐내느라 1번(Timer) 정거장이나 4번(Poll) 정거장으로 영원히 버스를 출발시키지 못합니다. 데이터베이스 응답이나 유저 접속 요청이 평생 대기 상태에 빠져 굶어 죽는 **기아 현상(Starvation)**이 발생합니다.
