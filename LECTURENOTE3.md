# 식당 웨이터 비유로 통찰하는 비동기 논블로킹 아키텍처

### 1. 멀티 스레드와 동기식 블로킹 (Synchronous Blocking)의 한계

- 전통적인 서버 아키텍처(Apache, Tomcat 등)는 사용자 요청이 들어올 때마다 새로운 '웨이터(스레드)'를 생성하여 1:1로 전담 마크

- 주문을 받은 웨이터는 요리(데이터베이스 조회 등)가 주방에서 나올 때까지 그 자리에 멈춰 서서 대기합니다(Blocking)

- 손님이 1,000명이면 웨이터도 1,000명이 필요합니다. 스레드가 생성될 때마다 독립적인 메모리 공간(Thread Stack)이 할당되므로, 트래픽이 몰리면 순식간에 RAM이 고갈되어 서버가 다운(Out of Memory)

- CPU는 한 번에 하나의 일만 할 수 있습니다. 수많은 스레드가 동시에 일하는 것처럼 보이는 이유는 CPU가 이들을 수 밀리초 단위로 전환하며 실행하기 때문입니다. 하던 일을 멈추고 다른 스레드의 상태를 불러오는 이 과정을 '컨텍스트 스위칭(Context Switching)'이라 합니다. 스레드가 많아질수록 실제 요리를 서빙하는 시간보다 교대하는 데 CPU 연산력을 다 써버려 시스템 전체가 멈추는 쓰레싱(Thrashing) 현상이 발생

🌱 직관적 비유
요리사(CPU) 한 명이 1,000개의 냄비를 동시에 관리하는 상황입니다. 1번 냄비를 젓고, 바로 뛰어내려가 2번 냄비 불을 줄이고, 다시 3번 냄비 간을 봅니다. 이처럼 냄비 사이를 미친 듯이 뛰어다니는 행위 자체가 '컨텍스트 스위칭'입니다. 나중에는 요리하는 시간보다 뛰어다니는 데 힘을 다 써버려(오버헤드) 음식을 하나도 완성하지 못하는 최악의 상황이 벌어집니다.

---

### 2. 싱글 스레드와 비동기 논블로킹 (Asynchronous Non-blocking)의 우아함

- 웨이터는 주문을 주방에 넘긴 뒤, 요리가 완성되기를 기다리지 않고(Non-blocking) 즉시 다음 테이블로 이동하여 새로운 주문

- 주방에서 요리가 완성되면 컨베이어 벨트(이벤트 큐)에 음식을 올립니다. 웨이터는 홀을 돌면서 틈틈이 이 벨트를 확인하여 완성된 순서대로 손님에게 서빙

- 단일 스레드라고 해서 혼자서 모든 일을 감당하는 것은 아닙니다. 파일 I/O나 네트워크 통신 같은 무거운 작업은 Node.js가 직접 처리하지 않고, 커널이 제공하는 고성능 이벤트 통지 API인 epoll(Linux), kqueue(macOS) 등의 시스템 콜을 통해 운영체제에게 철저하게 위임(Offloading)합니다. 메인 스레드는 교통정리만 수행하고, 실제 무거운 짐은 OS 커널이 백그라운드에서 나르는 구조

- 🌱 직관적 비유
  웨이터가 혼자 일한다고 해서 배추 100포기를 직접 나르는 것이 아닙니다. 주문만 빠르게 받고, 무거운 짐을 나르는 일은 건물 관리소 직원(운영체제)에게 "이것 좀 창고에 넣어주세요!" 하고 떠넘겨버리는(위임) 것입니다. 덕분에 웨이터는 가벼운 몸놀림으로 계속 손님만 받을 수 있습니다.

---

### 3. CPU 집약적 작업(CPU-bound Tasks)의 정의와 종류

- 작업의 완료 시간이 네트워크나 디스크의 속도가 아닌, CPU의 연산 속도(클록 스피드)에 의해 결정되는 작업입니다. 외부 응답을 기다리는 대기 시간 없이 오로지 CPU가 쉬지 않고 연산해야 하는 일

- 실무에서의 구체적인 종류:
  암호화 및 해싱: 비밀번호 암호화(bcrypt), 대용량 데이터 복호화
  이미지 및 동영상 처리: 이미지 리사이징(sharp), 동영상 인코딩 및 트랜스코딩
  대규모 데이터 파싱: 수십만 줄의 JSON 객체 변환(JSON.parse), XML 파싱
  압축 및 해제: 대용량 파일을 ZIP이나 Gzip으로 압축하는 과정
  복잡한 수학적 계산: AI 모델 추론, 대규모 배열 정렬, 통계 수치 산출

  Node.js 메인 스레드는 이러한 작업들을 직접 처리하기에 적합하지 않습니다. 웨이터가 주문을 받는 대신 주방 구석에 앉아 직접 복잡하게 양파를 썰고 있는 꼴이 되기 때문

---

### 4. 치명적 안티 패턴: 이벤트 루프 블로킹 (양파 썰기)

- 비동기 환경에서 가장 경계해야 할 것은 메인 스레드가 직접 'CPU 집약적 작업'을 수행하도록 만드는 것. 주방(OS/백그라운드)에 맡길 수 없는 복잡한 연산을 메인 스레드에 지시하면, 메인 스레드가 그 자리에서 연산이 끝날 때까지 멈추게 됩니다.유일한 웨이터가 멈췄으므로 이벤트 루프 전체가 마비되며, 새로 인입되는 다른 모든 사용자의 요청(로그인, API 호출 등)이 응답 불가 상태(Blocking)

💡 CS 실무 Point (CPU 집중 작업의 격리)
현업에서는 이를 해결하기 위해 worker_threads 모듈을 사용해 완전히 분리된 별도의 자바스크립트 실행 환경(워커 스레드)을 띄우거나, Node.js 서버는 가벼운 API 게이트웨이 역할만 수행하도록 하고 무거운 연산은 메시지 큐(AWS SQS 등)를 통해 Python/Go 기반의 독립된 마이크로서비스로 넘겨버리는 아키텍처를 설계합니다.

### 5. 코드 레벨에서의 동작 원리와 콜백(Callback)의 본질

안티 패턴: 동기(Synchronous) 블로킹 방식
웨이터가 주방 앞에서 요리가 나올 때까지 멈춰버립니다. 다음 라인의 코드는 앞선 조회가 끝나기 전까지 절대 실행되지 않습니다.

```javascript
const data = db.querySync("SELECT * FROM users");
console.log("데이터 조회가 끝났습니다.", data);
```

추천 패턴: 비동기(Asynchronous) 논블로킹 방식

```javascript
db.query("SELECT * FROM users", (error, data) => {
  // 훗날 요리가 완성되어 주방에서 진동벨을 울리면, 그때 돌아와서 이 서빙(함수)을 실행합니다.
  console.log("요청하신 데이터가 준비되었습니다.", data);
});

console.log("주문을 주방에 넣었고, 저는 곧바로 다음 테이블로 이동합니다!"); // 즉시 실행됨!
```

---

### 6. 핵심 용어 사전 (Glossary)

#### 6-1) 동기 (Synchronous) vs 비동기 (Asynchronous)

- 동기 (Synchronous): 호출된 함수의 작업 완료 시점과 다음 작업의 시작 시점을 맞추는 방식입니다. A 함수가 실행 중일 때 B 함수는 A의 결과가 나올 때까지 기다리거나, 주기적으로 완료되었는지 확인(Polling)하며 실행 순서를 엄격하게 보장합니다.

- 비동기 (Asynchronous): 호출된 함수의 완료 시점을 호출한 쪽에서 신경 쓰지 않는 방식입니다. 작업을 시작하라는 명령과 함께 나중에 처리할 일(Callback, Promise 등)을 쥐여주고, 호출한 쪽은 곧바로 제 갈 길을 갑니다. 작업의 완료와 서빙은 미래의 어느 시점에 독립적으로 일어납니다.

#### 6-2) 블로킹 (Blocking) vs 논블로킹 (Non-blocking)

- 블로킹 (Blocking): 호출된 함수가 자신의 작업을 끝낼 때까지 제어권을 꽉 쥐고 돌려주지 않는 것입니다. 호출한 함수는 제어권이 없기 때문에 아무 일도 하지 못하고 그 줄에서 실행이 멈춥니다(Block).

- 논블로킹 (Non-blocking): 호출된 함수가 작업을 다 마치지 못했더라도, 호출되자마자 제어권을 즉시 돌려주는 것입니다. 제어권을 곧바로 반환받은 호출 측은 대기하지 않고 다음 줄의 코드를 계속 실행할 수 있습니다.

```docs
### 💡 면접 핵심: 비동기(Asynchronous)와 논블로킹(Non-blocking)은 엄연히 다릅니다!

* **동기/비동기**는 '작업 완료를 누가 신경 쓰고 시점을 맞추느냐'의 문제이고,
* **블로킹/논블로킹**은 '호출된 함수가 제어권을 바로 돌려주느냐 쥐고 있느냐'의 문제입니다.

이를 바탕으로 한 4가지 조합의 실무 맥락은 다음과 같습니다.

1. **동기 + 블로킹 (Sync + Blocking)**
   - **특징**: 제어권을 넘겨주고 작업이 끝날 때까지 멈춰 서서 기다립니다.
   - **실무**: 가장 일반적인 코드로, Java/Spring에서 데이터베이스 조회가 끝날 때까지 스레드가 멈추는 상황입니다.

2. **비동기 + 논블로킹 (Async + Non-blocking)**
   - **특징**: 작업을 맡기자마자 제어권을 즉시 돌려받아 다음 코드를 실행하고, 결과는 미래에 이벤트(콜백)로 통지받습니다.
   - **실무**: Node.js의 핵심 아키텍처로, 싱글 스레드가 수만 개의 I/O를 멈춤 없이 처리하는 비결입니다.

3. **동기 + 논블로킹 (Sync + Non-blocking)**
   - **특징**: 제어권을 즉시 돌려받아 내 할 일을 하지만, 작업이 끝났는지는 본인이 주기적으로 계속 물어봅니다(Polling).
   - **실무**: OS 소켓 통신의 `O_NONBLOCK` 상태나, 프론트엔드에서 서버에 3초마다 "새 데이터 있나요?" 하고 HTTP 요청을 보내는 폴링 기술입니다.

4. **비동기 + 블로킹 (Async + Blocking)**
   - **특징**: 결과는 나중에 알아서 주겠다고(비동기) 해놓고, 정작 제어권을 안 돌려주거나 다른 곳에서 강제로 묶여 있는(블로킹) 비효율적인 상황입니다.
   - **실무**: Node.js 개발자가 비동기 DB 드라이버를 쓰면서, 정작 OS 레벨에서 논블로킹을 지원하지 않는 옛날 드라이버나 파일 시스템을 거쳐 실행이 막혀버리는 일종의 '오작동/안티패턴' 맥락에서 자주 언급됩니다.

```

#### 6-3) 이벤트 루프 (Event Loop)와 이벤트 큐 (Event Queue)

- 이벤트 큐 (Event Queue / Task Queue): 비동기 작업(타이머, 네트워크 요청, 파일 I/O 등)이 백그라운드에서 완료된 후, 메인 스레드에서 실행되기를 기다리는 콜백 함수들이 차례대로 줄을 서는 컨베이어 벨트(FIFO)입니다.

- 이벤트 루프 (Event Loop): 메인 스레드 내부에서 "호출 스택(Call Stack)이 비어 있는지"와 "이벤트 큐에 대기 중인 콜백이 있는지"를 끊임없이 감시하는 무한 루프입니다. 만약 호출 스택이 완전히 비어 있다면, 큐의 맨 앞에 있는 콜백을 스택으로 끌어올려 메인 스레드가 실행할 수 있도록 배정

#### 6-4) 스레드 (Thread)와 컨텍스트 스위칭 (Context Switching)

- 스레드 (Thread): 운영체제가 CPU 시간을 할당하는 프로그램 실행 흐름의 최소 단위입니다. 프로세스 내에서 Stack 영역만 독립적으로 할당받고 코드(Code), 데이터(Data), 힙(Heap) 메모리 영역은 다른 스레드들과 공유하므로 가볍게 통신

- 컨텍스트 스위칭 (Context Switching): CPU가 멀티태스킹을 위해 현재 실행 중인 스레드의 상태(레지스터 값, 프로그램 카운터 등)를 TCB(Thread Control Block)에 저장하고, 새로 실행할 스레드의 상태를 복구하는 과정입니다. 이 교체 작업을 하는 동안 CPU는 실제 비즈니스 연산을 하지 못하므로, 스레드가 과도하게 많아지면 시스템이 멈추는 쓰레싱(Thrashing) 오버헤드가 발생

#### 6-5) I/O 집약적 (I/O Bound) vs CPU 집약적 (CPU Bound)

- I/O 집약적 (I/O Bound): 프로그램의 실행 속도가 디스크 읽기/쓰기, 네트워크 송수신, 데이터베이스 쿼리 응답 등 외부 자원의 속도에 의해 제한되는 경우입니다. CPU는 대부분의 시간을 데이터를 기다리는 '유휴(Idle) 상태'로 보냅니다. (예: 대규모 채팅 서버, API 게이트웨이, 웹 크롤러)

- CPU 집약적 (CPU Bound): 프로그램의 속도가 CPU의 순수 연산 속도(클록, 코어 성능)에 의해 제한되는 경우입니다. 대기 시간 없이 CPU가 100% 가동되며 수학적 연산을 쏟아내야 합니다. (예: 이미지 리사이징, 암호 화폐 채굴, 동영상 인코딩, 머신러닝 모델 추론)

#### 6-6) 시스템 콜 (System Call) 및 OS I/O 다중화 (Multiplexing)

- 시스템 콜 (System Call): 사용자 프로그램이 하드웨어 자원(파일 읽기, 네트워크 소켓 열기 등)에 직접 접근하는 것은 보안상 금지되어 있습니다. 따라서 운영체제 커널(Kernel)에게 안전하게 권한을 대행해 달라고 요청하는 인터페이스입니다. 시스템 콜이 일어나면 CPU는 유저 모드에서 커널 모드로 전환

- OS I/O 다중화 (Multiplexing): 단 하나의 스레드가 수천, 수만 개의 네트워크 연결(소켓 파일 디스크립터)을 동시에 효율적으로 감시하는 기술

- 전통적인 방식은 모든 소켓을 돌며 데이터가 왔는지 물어보는 무식한 방식(Polling)이었습니다.

- 현대 OS의 다중화 기술(Linux의 epoll, macOS의 kqueue, Windows의 IOCP)은 관할 소켓에 데이터가 들어오면 커널이 이벤트 인터럽트를 발생시켜 소켓 레이어에 직접 통지(Notify)해 줍니다. Node.js는 이 커널 이벤트를 받아 가볍게 큐에 실어 나르는 구조이기 때문에 싱글 스레드로 대규모 연결을 소화할 수 있습니다.
