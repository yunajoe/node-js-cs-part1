# NPM 패키지 완벽 제어와 개발용 의존성(DevDependencies)의 이해

### 1. 패키지 확인 및 정보 조회

| 목적                 | 명령어                         | 설명                                                                       |
| :------------------- | :----------------------------- | :------------------------------------------------------------------------- |
| **설치 패키지 목록** | `npm list --depth=0`           | 하위 의존성을 제외하고 `package.json`에 명시된 최상위 패키지만 트리에 출력 |
| **패키지 상세 정보** | `npm view <패키지명>`          | 최신 버전, 배포일, 라이선스 등 상세 정보 조회                              |
| **과거 버전 이력**   | `npm view <패키지명> versions` | 해당 패키지가 거쳐온 전체 버전 목록 출력                                   |

---

### 2. 특정 버전 설치, 업데이트, 패키지 삭제

- 특정 버전 설치 (Rollback)

```javascript

npm install chalk@4.1.2
// 버전 충돌이나 버그 발생 시 @버전을 명시하여 과거의 안정적인 버전으로 다운로드합니다.

```

- 안전한 일괄 업데이트

```javascript
npm update
// package.json에 명시된 범위(Caret ^ 등) 내에서 하위 호환성을 유지하며 마이너/패치 버전을 최신으로 끌어올립니다.

```

- 패키지 완벽 삭제

```javascript
npm uninstall chalk
// node_modules 폴더 내 파일 제거 및 package.json, package-lock.json 기록까지 깔끔하게 삭제합니다.

```

---

### 3. 보안 점검 (npm audit)

```javascript
보안 취약점 검사: npm audit
취약점 자동 패치: npm audit fix // 설치된 패키지의 알려진 보안 이슈를 점검하고, 패치된 안전한 버전으로 자동 교체합니다.
```

---

### 4. 개발용 의존성 (DevDependencies)

- package.json 버전 표기 앞의 기호는 자동 업데이트 범위를 지정

```
Dependencies (일반 의존성): 실제 프로덕션 서버 운영 환경에서 필수적인 패키지 (예: UI 라이브러리, 로깅 도구 등).

DevDependencies (개발용 의존성): 개발 단계에서만 편의·테스트 용도로 사용하는 도구. 빌드/배포 시 제외하여 서버 용량 및 메모리를 절약함.

```
